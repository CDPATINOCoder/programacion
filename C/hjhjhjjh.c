#include <stdio.h>
#include <math.h>

int main()
{
    double x1, y1, x2, y2, d;
    printf("ingrese los valres positivos de los puntods");
    scanf("%f", &x1);
    scanf("%f", &y1);
    scanf("%f", &x2);
    scanf("%f", &y2);
    if (x1 >= 0 && y1 >= 0)

        else
        {
            printf("no es del primer cuadrante");
        }

    return 0;
}